<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productivitycac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/login_activity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ToDoList"
            android:label="to_do_list_activity"

            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ToDoList1"
            android:label="to_do_list_activity1"

            android:theme="@style/AppTheme.NoActionBar" />


        <activity
            android:name=".CreateNewTaskActivity"
            android:label="@string/create_new_task_activity"

            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserDashboard"
            android:label="@string/user_dashboard_activity"

            android:theme="@style/AppTheme.NoActionBar">



        </activity>
        <activity android:name=".list_of_lists" />

        <activity android:name=".StopwatchActivity"/>

    </application>

</manifest>